name: Deploy PostgreSQL Docker Compose

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: dev
        type: choice
        options:
          - dev
          - test
          - prod

jobs:

  checkout_and_transfer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Copy project to VPS
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "./infra/docker/docker-compose.yml,./infra/sql/init.sh"
          target: "/home/${{ secrets.VPS_USER }}/"

      # Move file into /opt using sudo
      - name: Move file to /opt (requires sudo)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            sudo mv /home/${{ secrets.VPS_USER }}/infra/docker/docker-compose.yml /opt/postgres/
            sudo mv /home/${{ secrets.VPS_USER }}/infra/sql/init.sh /opt/postgres/
            sudo chown -R $USER:$USER /opt/postgres

  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    needs: checkout_and_transfer

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /opt/postgres

            export POSTGRES_USER=${{ secrets.POSTGRES_USER }}
            export POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
            export POSTGRES_PORT=${{ secrets.POSTGRES_PORT }}
            export CONTAINER_NAME=${{ vars.CONTAINER_NAME }}
            export APP_ENV=${{ github.event.inputs.environment }}
            export POSTGRES_DB='${{ secrets.POSTGRES_DB }}'

      
            docker compose -p ${{ vars.COMPOSE_PROJECT_NAME }} -f docker-compose.yml up -d
            echo "Deployment complete"